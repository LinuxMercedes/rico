Hash map
<(attribute), (List<possible value>)>
<(attribute name, attribute class), (List<Partition*>)>

* - Partition: List<Instances>

==============================

getPartitions() << hot patch into rarff

minimal()

isLessThan(Partition p)

getCovering(Partition p)

==============================

add patch to Attribute class in Rarff -> id

