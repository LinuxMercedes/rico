+ Read arff file (rarff)
+ Choose partition and decision attributes
- Specify min and max for partition sizes
- Specify whether or not to prune unecessary conditions
- Command line options? (getopt?)
- Calculate partitions (hash of arrays?)
- Determine if partitions are equivalent (O(awful))
- Determine if a partition is minimal (O(worse))
- Induce rules from partitions ("easy")
- Remove unecessary conditions from rules
- DOCUMENT
x Support for partitions on numeric data? (everything looks integer so we can treat it as discrete)
